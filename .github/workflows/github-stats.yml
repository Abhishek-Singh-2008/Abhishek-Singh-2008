name: Generate GitHub Stats Images
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch: {}
  push:
    branches:
      - main

# This workflow is now kept minimal.
# Stats cards (overview + languages) are served via github-readme-stats API directly in README.
# This workflow can be used for future automation if needed.

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Stats are now served via github-readme-stats API. No generation needed."
